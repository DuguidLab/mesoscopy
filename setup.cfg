[metadata]
name = mesoscopy
version = 0.0.1

[options]
install_requires =
    Click
    importlib; python_version == "3.8"
package_dir=
    =src
packages=find:

[options.packages.find]
where=src

[options.entry_points]
console_scripts =
        mesoscopy = mesoscopy:cli